spring:
  application:
    name: research-feign-1
  config:
    discovery:
      enabled: true
      serviceId: xs-blog-config-server
    profile: default
  cloud:
    config:
      discovery:
        enabled: true
        service-id: xs-blog-config-server
      profile: default
      label: master
      name: research-feign-1,mybatis-mac

server:
  port: 2220

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

logging:
  level:
    root: INFO