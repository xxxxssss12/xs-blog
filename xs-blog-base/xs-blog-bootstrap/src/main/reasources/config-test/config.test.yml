server:
  port: 8800
spring:
  application:
    name: research-feign
  config:
    profile: default
    discovery:
      enabled: true
      serviceId: xs-blog-config-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: xs-blog-config-server
      profile: default
      label: master
      name: ${spring.application.name},mybatis-macs

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

logging:
  level:
    root: debug